#ifndef TF_PSA_CRYPTO_COMPAT_TIME_H
#define TF_PSA_CRYPTO_COMPAT_TIME_H

#include <stdint.h>

// 时间类型定义
typedef int64_t time_t;
typedef int64_t suseconds_t;

// 简化的时间函数（如果 mbedtls 需要）
// 这些函数可能需要在实际使用中实现

#endif // TF_PSA_CRYPTO_COMPAT_TIME_H
