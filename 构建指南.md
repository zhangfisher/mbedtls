# Mbed TLS Zig 构建指南

本指南介绍如何使用 Zig 构建系统构建 Mbed TLS 项目，支持多平台交叉编译。

## 📋 项目概述

本项目是 Mbed TLS 的 Zig 构建系统集成，提供以下功能：

- **多平台支持**: Windows x86-64、Linux ARM64、Linux ARM32、Linux x86-64 (musl/gnu)
- **WebRTC专用构建**: 针对WebRTC应用优化的精简版本
- **交叉编译**: 从 Windows 主机交叉编译到 Linux ARM/x86-64 平台
- **优化构建**: 使用 ReleaseSmall 优化模式
- **灵活构建**: 支持按需构建指定平台

## 🛠️ 前置要求

### 必需软件

1. **Zig 编译器**
   - 版本：0.16.0
   - 下载：https://ziglang.org/download/
   - 安装后验证：`zig version`

2. **Git**
   - 用于克隆代码库
   - 验证：`git --version`

### 系统要求

- **Windows 主机**: Windows 10 或更高版本
- **磁盘空间**: 至少 500MB 可用空间
- **内存**: 建议 4GB 以上

## 🚀 快速开始

### 1. 克隆代码库

```bash
git clone <repository-url>
cd mbedtls
```

### 2. 验证环境

确保 Zig 已正确安装：

```bash
zig version
# 应显示: 0.16.0
```

### 3. 执行构建

```bash
# 构建所有平台
zig build

# 查看可用构建步骤
zig build --help
```

### 4. 查看构建产物

```bash
# Windows
dir zig-out\lib

# Linux/macOS
ls -la zig-out/lib
```

## 📦 构建产物说明

### 标准构建产物

#### Windows x86-64
```
zig-out/lib/
├── tfpsacrypto_windows_x64.lib     # 密码学核心库 (206KB)
├── mbedx509_windows_x64.lib         # X.509证书处理 (7.5KB)
└── mbedtls_windows_x64.lib          # TLS协议实现 (15KB)
```

#### Linux ARM64
```
zig-out/lib/
├── libtfpsacrypto_linux_arm64.a     # 密码学核心库 (235KB)
├── libmbedx509_linux_arm64.a        # X.509证书处理 (9.0KB)
└── libmbedtls_linux_arm64.a         # TLS协议实现 (15KB)
```

#### Linux ARM32
```
zig-out/lib/
├── libtfpsacrypto_linux_arm32.a     # 密码学核心库 (182KB)
├── libmbedx509_linux_arm32.a        # X.509证书处理 (7.7KB)
└── libmbedtls_linux_arm32.a         # TLS协议实现 (13KB)
```

#### Linux x86-64 (musl)
```
zig-out/lib/
├── libtfpsacrypto_linux_x64_musl.a  # 密码学核心库 (227KB)
├── libmbedx509_linux_x64_musl.a     # X.509证书处理 (8.9KB)
└── libmbedtls_linux_x64_musl.a      # TLS协议实现 (15KB)
```

#### Linux x86-64 (gnu)
```
zig-out/lib/
├── libtfpsacrypto_linux_x64_gnu.a   # 密码学核心库 (227KB)
├── libmbedx509_linux_x64_gnu.a      # X.509证书处理 (8.9KB)
└── libmbedtls_linux_x64_gnu.a       # TLS协议实现 (15KB)
```

### WebRTC 专用构建产物

**重要说明**：WebRTC 构建产物通过**移除 WebRTC 应用不需要的功能**来实现体积优化。这些产物专门设计用于支持 WebRTC 的 DTLS 安全通信，不适合用于通用的 TLS/HTTPS 应用场景。

**移除的主要功能**：
- ❌ 不常用的密码算法（ARIA、Camellia、MD5、RIPEMD160、SHA-1、SHA-3）
- ❌ 复杂证书处理（证书写入、CRL 解析、PKCS#7）
- ❌ 硬件加速模块（AES-NI、NEON 等）
- ❌ 高级密码学功能（J-PAKE、FFDH、PAKE、XOF）

所有 WebRTC 专用库文件名都包含 `-webrtc` 后缀：

#### Windows x86-64
```
zig-out/lib/
├── tfpsacrypto_windows_x64_webrtc.lib
├── mbedx509_windows_x64_webrtc.lib
└── mbedtls_windows_x64_webrtc.lib
```

#### Linux ARM64
```
zig-out/lib/
├── libtfpsacrypto_linux_arm64_webrtc.a
├── libmbedx509_linux_arm64_webrtc.a
└── libmbedtls_linux_arm64_webrtc.a
```

#### Linux ARM32
```
zig-out/lib/
├── libtfpsacrypto_linux_arm32_webrtc.a
├── libmbedx509_linux_arm32_webrtc.a
└── libmbedtls_linux_arm32_webrtc.a
```

#### Linux x86-64 musl
```
zig-out/lib/
├── libtfpsacrypto_linux_x64_musl_webrtc.a
├── libmbedx509_linux_x64_musl_webrtc.a
└── libmbedtls_linux_x64_musl_webrtc.a
```

#### Linux x86-64 gnu
```
zig-out/lib/
├── libtfpsacrypto_linux_x64_gnu_webrtc.a
├── libmbedx509_linux_x64_gnu_webrtc.a
└── libmbedtls_linux_x64_gnu_webrtc.a
```

## 🎯 WebRTC 专用构建说明

### 什么是 WebRTC 专用构建？

**WebRTC 专用构建** 是针对 WebRTC 实时通信应用的特殊优化版本，通过**移除 WebRTC 不需要的功能**来显著减小构建产物大小。

**核心概念**：
- 🎯 **不是通用精简版本**，而是专门针对 WebRTC 用途的优化
- 🚫 **移除不常用算法**：WebRTC 握手中不使用的密码学算法
- 📦 **减少代码体积**：通过针对性功能裁剪，减小约 12% 的总大小
- ✅ **保持完整兼容性**：完整保留 WebRTC 需要的 DTLS 协议功能

### 功能优化

WebRTC 专用构建针对实时通信应用进行了优化：

- **体积减小**: 三层库总体减小约 12%，其中 X.509 库减小约 71%
- **功能裁剪**: 移除 WebRTC 不需要的功能（证书写入、CRL 解析、PKCS7 等）
- **密码学优化**: 移除不常用的对称加密、哈希算法和硬件加速模块
- **保留核心**: 完整保留 WebRTC 必需的 TLS/DTLS 协议和密码学功能

### WebRTC 证书处理机制

WebRTC 使用 DTLS 协议进行安全通信，需要 X.509 证书库支持，但通过优化大幅减小了库体积。

#### 为什么 WebRTC 需要 X.509 支持？

虽然 WebRTC 使用自签名证书，但仍需要 X.509 功能：

1. **DTLS 握手流程**：需要解析对端证书（即使是自签名的）
2. **指纹验证**：WebRTC 通过证书指纹匹配验证身份
3. **证书结构解析**：需要提取公钥、指纹等信息

#### WebRTC mbedx509 优化详情

**优化原理**：通过分析 WebRTC 应用的实际使用场景，**识别并移除 WebRTC 不需要的 X.509 功能**。WebRTC 使用自签名证书进行 DTLS 握手，不需要复杂的证书验证和高级功能，因此可以大幅简化 X.509 库。

**标准版本 vs WebRTC 版本对比**：

| 平台 | 标准 mbedx509 | WebRTC mbedx509 | 优化率 |
|------|---------------|-----------------|--------|
| Linux ARM64 | 9.0KB | **2.5KB** | **72.2%** |
| Linux ARM32 | 7.7KB | **2.2KB** | **71.4%** |
| Linux x64 musl | 8.9KB | **2.5KB** | **71.9%** |
| Linux x64 gnu | 8.9KB | **2.5KB** | **71.9%** |
| Windows x64 | 7.5KB | **2.2KB** | **70.7%** |

**WebRTC mbedx509 保留功能**：
- ✅ 基本 X.509 结构定义
- ✅ DER 解析核心功能
- ✅ OID 识别（最小集）
- ✅ 证书基本信息提取

**WebRTC mbedx509 移除功能**：
- ❌ 证书写入功能
- ❌ CRL 解析
- ❌ PKCS#7 支持
- ❌ 证书链复杂验证
- ❌ 扩展字段高级处理

#### 三层库总体优化效果

完整的 WebRTC 构建三层库优化对比：

| 库层次 | 标准版本 | WebRTC版本 | 优化率 | 重要性 |
|--------|----------|------------|--------|--------|
| **tfpsacrypto** | 235KB | **212KB** | **9.8%** | 🔥 密码学核心 |
| **mbedtls** | 15KB | **14KB** | **5.2%** | TLS 协议层 |
| **mbedx509** | 9.0KB | **2.5KB** | **72.2%** | 🎯 证书处理层 |
| **总计** | **259KB** | **228.5KB** | **11.8%** | — |

**大小贡献分析**：
```
WebRTC 总大小: 228.5KB
├── tfpsacrypto: 212KB (92.8%)
├── mbedtls: 14KB (6.1%)
└── mbedx509: 2.5KB (1.1%) ← 已充分优化
```

#### 为什么不移除 mbedx509？

虽然 WebRTC mbedx509 只占总体的 1.1%，但不建议完全移除：

**技术原因**：
1. **DTLS 协议要求**：DTLS 握手必须解析对端证书结构
2. **代码耦合度高**：TLS/DTLS 代码深度依赖 X.509 API
3. **替代方案成本高**：需要重写 DTLS 证书处理逻辑

**风险分析**：
- ❌ 可能引入安全风险
- ❌ 维护成本显著增加
- ❌ WebRTC 标准兼容性问题

**替代方案局限性**：
- **PSK 模式**：不需要 X.509，但需要提前协商密钥，不适合 WebRTC 的 ad-hoc 场景
- **RawPublicKey**：避免完整 X.509 解析，但需要额外配置，WebRTC 支持度有限

### 详细的 WebRTC 功能说明

请参阅构建产物目录中的功能摘要文档：

```bash
# Windows
notepad zig-out\lib\WebRTC构建功能摘要.md

# Linux/macOS
cat zig-out/lib/WebRTC构建功能摘要.md
```

## 🔧 高级构建选项

### 仅构建特定平台

使用命令行选项按需构建特定平台：

```bash
# 只构建 Windows x86-64
zig build -Dplatform=windows

# 只构建 Linux ARM64
zig build -Dplatform=linux-arm64

# 只构建 Linux ARM32
zig build -Dplatform=linux-arm32

# 只构建 Linux x86-64 musl
zig build -Dplatform=linux-x64-musl

# 只构建 Linux x86-64 gnu
zig build -Dplatform=linux-x64-gnu

# 构建多个平台（用逗号分隔）
zig build -Dplatform=windows,linux-arm64

# 构建特定平台的 WebRTC 版本
zig build -Dplatform=windows -Dwebrtc=true

# 构建所有平台（默认）
zig build
```

**选项说明**：
- `-Dplatform`：指定目标平台（`windows`、`linux-arm64`、`linux-arm32`、`linux-x64-musl`、`linux-x64-gnu` 或 `all`）
- `-Dwebrtc`：构建 WebRTC 优化版本（布尔值）
- 支持多个平台用逗号分隔：`-Dplatform=windows,linux-arm64`

**性能对比**：
| 操作 | 构建时间 | 产物大小 | 节省 |
|------|----------|----------|------|
| `zig build -Dplatform=windows` | ~1分钟 | 232K | **80% 时间 / 85% 空间** |
| `zig build -Dplatform=windows,linux-arm64` | ~2分钟 | 500K | **60% 时间 / 58% 空间** |
| `zig build -Dplatform=windows -Dwebrtc=true` | ~1分钟 | 204K | **80% 时间 / 87% 空间** |
| `zig build`（默认） | ~5分钟 | 1.2M | - |

### 清理构建缓存

```bash
# 清理所有构建产物和缓存
rm -rf zig-out
rm -rf zig-cache
```

### 增量构建

Zig 支持增量构建，只重新编译修改的文件：

```bash
# 每次构建都是增量的
zig build
```

## 💡 使用示例

### C 项目中链接标准版本

#### Windows (MinGW)

```c
// main.c
#include <mbedtls/ssl.h>
#include <mbedtls/net_sockets.h>

int main() {
    mbedtls_ssl_context ssl;
    mbedtls_ssl_init(&ssl);
    // ... 使用 TLS 功能
    mbedtls_ssl_free(&ssl);
    return 0;
}
```

编译命令：

```bash
zig cc -o myapp.exe main.c \
    -I"include" \
    -I"tf-psa-crypto/include" \
    "zig-out/lib/mbedtls_windows_x64.lib" \
    "zig-out/lib/mbedx509_windows_x64.lib" \
    "zig-out/lib/tfpsacrypto_windows_x64.lib" \
    -lws2_32 -ladvapi32
```

#### Linux ARM64 交叉编译

```bash
zig cc -target aarch64-linux-musl -o myapp main.c \
    -Iinclude \
    -Itf-psa-crypto/include \
    zig-out/lib/libmbedtls_linux_arm64.a \
    zig-out/lib/libmbedx509_linux_arm64.a \
    zig-out/lib/libtfpsacrypto_linux_arm64.a
```

### Zig 项目中集成

创建 `my_project/src/main.zig`：

```zig
const std = @import("std");
const c = @cImport({
    @cInclude("mbedtls/ssl.h");
    @cInclude("mbedtls/net_sockets.h");
});

pub fn main() !void {
    var ssl: c.mbedtls_ssl_context = undefined;
    c.mbedtls_ssl_init(&ssl);
    // ... 使用 TLS 功能
    c.mbedtls_ssl_free(&ssl);
}
```

创建 `my_project/build.zig`：

```zig
const std = @import("std");

pub fn build(b: *std.Build) void {
    const target = b.standardTargetOptions(.{});
    const optimize = b.standardOptimizeOption(.{});
    
    const exe = b.addExecutable(.{
        .name = "myapp",
        .root_source_file = .{ .path = "src/main.zig" },
        .target = target,
        .optimize = optimize,
    });
    
    // 添加头文件搜索路径
    exe.addIncludePath(.{ .path = "../mbedtls/include" });
    exe.addIncludePath(.{ .path = "../mbedtls/tf-psa-crypto/include" });
    
    // 链接 Mbed TLS 库
    exe.linkLibrary(b.addStaticFile(.{ .path = "../mbedtls/zig-out/lib/mbedtls_windows_x64.lib" }));
    exe.linkLibrary(b.addStaticFile(.{ .path = "../mbedtls/zig-out/lib/mbedx509_windows_x64.lib" }));
    exe.linkLibrary(b.addStaticFile(.{ .path = "../mbedtls/zig-out/lib/tfpsacrypto_windows_x64.lib" }));
    
    exe.linkSystemLibrary("ws2_32");
    exe.linkSystemLibrary("advapi32");
    
    b.installArtifact(exe);
}
```

## 🔍 故障排除

### 常见问题

#### 1. Zig 版本不匹配

**错误**: `zig version` 显示不是 0.16.0

**解决**: 
```bash
# 下载 Zig 0.16.0
# Windows: 下载 zip 文件并解压
# 添加到 PATH 环境变量
```

#### 2. 编译错误

**错误**: `error: unable to find C source files`

**解决**: 确保在项目根目录执行构建：
```bash
cd mbedtls
zig build
```

#### 3. 内存不足

**错误**: 编译过程中系统内存不足

**解决**: 
- 关闭其他应用程序
- 增加 Windows 虚拟内存大小
- 只构建需要的平台

#### 4. Windows 安全软件警告

**错误**: 杀毒软件阻止 zig.exe 运行

**解决**: 
- 将 zig.exe 添加到杀毒软件白名单
- 临时禁用杀毒软件进行构建

### 获取构建详情

查看详细的构建过程：

```bash
zig build --summary all
```

查看详细的编译命令：

```bash
zig build -fcompile_flags
```

## 📚 库依赖关系

### 链接顺序

使用 Mbed TLS 库时，必须按以下顺序链接：

```
mbedtls → mbedx509 → tfpsacrypto
```

### Windows 系统库依赖

Windows 版本需要链接以下系统库：

- `ws2_32.lib` - Windows Sockets
- `advapi32.lib` - 高级 Windows API

## 🎓 构建系统架构

### 三层库架构

```
┌─────────────────┐
│   mbedtls      │  ← TLS 协议层
├─────────────────┤
│   mbedx509     │  ← X.509 证书层
├─────────────────┤
│  tfpsacrypto   │  ← 密码学核心层
└─────────────────┘
```

### 源文件组织

- **tf-psa-crypto/**: PSA 密码学 API 实现
- **library/**: Mbed TLS 核心实现
- **include/**: 公开头文件
- **build.zig**: Zig 构建配置

## 🔗 相关资源

### 官方文档

- [Mbed TLS 官方文档](https://tls.mbed.org/)
- [Mbed TLS API 文档](https://tls.mbed.org/api/)
- [Zig 官方文档](https://ziglang.org/documentation/0.16.0/)
- [PSA 密码学 API 规范](https://arm-software.github.io/psa-api/)

### 社区资源

- [Zig 中文社区](https://zigcc.github.io/)
- [Mbed TLS GitHub](https://github.com/Mbed-TLS/mbedtls)

## 📄 许可证

Mbed TLS 采用双重许可：
- Apache License 2.0
- GPL v2 或更高版本

## 🤝 贡献

欢迎贡献！请遵循以下步骤：

1. Fork 项目
2. 创建特性分支
3. 提交更改
4. 推送到分支
5. 创建 Pull Request

## 📞 技术支持

如有问题或建议，请：

1. 查看本指南的故障排除部分
2. 检查 `zig-out/lib/WebRTC构建功能摘要.md`
3. 查阅官方文档
4. 提交 Issue 或 Pull Request

---

## ⚠️ WebRTC 构建重要说明

**WebRTC 专用构建的本质**：本项目提供的 WebRTC 构建产物是**针对 WebRTC 实时通信应用优化的专门版本**，通过**移除 WebRTC 应用场景中不需要的功能**来显著减小代码体积。

**适用场景**：
- ✅ WebRTC 实时通信应用（视频/音频通话、数据传输）
- ✅ 使用自签名证书的 DTLS 安全通信
- ✅ 嵌入式设备的 WebRTC 客户端

**不适用场景**：
- ❌ 通用 HTTPS/TLS 服务器
- ❌ 需要完整证书链验证的应用
- ❌ 需要复杂证书管理的企业应用
- ❌ 需要支持已弃用密码算法的遗留系统

**体积优化来源**：
- **密码学算法优化**：移除 WebRTC 不常用的对称加密、哈希算法（~10% 优化）
- **X.509 功能裁剪**：移除证书写入、CRL 解析等高级功能（~72% 优化）
- **TLS 协议精简**：保留 DTLS 握手必需的核心功能（~5% 优化）
- **总体优化效果**：三层库总体减小约 12%

**选择建议**：
- 如果您的应用是 WebRTC 相关 → 使用 WebRTC 构建版本（带 `-webrtc` 后缀）
- 如果您的应用是通用 TLS/HTTPS → 使用标准构建版本

**构建日期**: 2026-06-04  
**Zig 版本**: 0.16.0  
**构建模式**: ReleaseSmall  
**支持平台**: Windows x86-64, Linux ARM64, Linux ARM32, Linux x86-64 (musl/gnu)
